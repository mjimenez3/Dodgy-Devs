import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  // new nerd
  rocket: {
    width: 50,
    height: 100,
    position: "absolute",
  },

  computer: {
    position: "absolute",
  },

  email: {
    width: 50,
    height: 90,
    position: "absolute",
  },

  duck: {
    width: 75,
    height: 75,
    position: "absolute",
  },
  codeSym: {
    position: "absolute",
  },
});

export default styles;
